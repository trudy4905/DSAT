<UserControl x:Class="WpfApp.Views.ScanOverlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="880" d:DesignWidth="1440">
    <!-- SCAN PROGRESS MODAL OVERLAY -->
    <Grid Visibility="{Binding IsScanOverlayVisible, Converter={StaticResource BoolToVisConverter}}" Panel.ZIndex="100">
        <Border Background="#800F172A" />
        <Border Background="#FFFFFF" Width="480" HorizontalAlignment="Center" VerticalAlignment="Center" Padding="32,28" CornerRadius="12">
            <StackPanel>
                <!-- 아이콘 -->
                <Border Width="52" Height="52" CornerRadius="26" HorizontalAlignment="Center" Margin="0,0,0,16">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="#F0F7FF"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsScanning}" Value="False">
                                    <Setter Property="Background" Value="#FEF3C7"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="🔍"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsScanning}" Value="False">
                                        <Setter Property="Text" Value="📭"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Border>

                <!-- 제목: 스캔 중 / 완료 -->
                <TextBlock FontSize="18" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" Margin="0,0,0,6">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="HWP / HWPX / PDF 파일 탐색 중..."/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsScanning}" Value="False">
                                    <Setter Property="Text" Value="탐색 완료"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- 상태 메시지 -->
                <TextBlock Text="{Binding ScanStatusMessage}" FontSize="12" Foreground="#64748B" HorizontalAlignment="Center" TextTrimming="CharacterEllipsis" Margin="0,0,0,16"/>

                <!-- 프로그레스바: 스캔 중에만 표시 -->
                <ProgressBar Height="8" IsIndeterminate="True" Background="#E2E8F0" Foreground="#3B82F6" BorderThickness="0" Margin="0,0,0,8"
                             Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisConverter}}"/>

                <!-- 진행/결과 텍스트: 스캔 중엔 발견 수 -->
                <TextBlock Text="{Binding ScanProgressText}" FontSize="13" FontWeight="Bold" Foreground="#16A34A" HorizontalAlignment="Center" Margin="0,0,0,20">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="#16A34A"/>
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ScanProgressText}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- 버튼 영역: 스캔 중엔 취소, 완료(결과 없음)엔 닫기 -->
                <Button Content="탐색 취소" Command="{Binding CancelScanCommand}" Style="{StaticResource SecondaryBtn}" HorizontalAlignment="Center" Width="120"
                        Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisConverter}}"/>
                <Button Content="닫기" Command="{Binding CloseOverlayCommand}" HorizontalAlignment="Center" Width="120"
                        Visibility="{Binding IsScanning, Converter={StaticResource BoolToInvVisConverter}}"
                        Background="#3B82F6" Foreground="White" FontWeight="SemiBold" FontSize="14"
                        Padding="16,8" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="6"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
